# Flutter client configuration only. Copy to .env and fill both values.
# Run the Flutter app with: flutter run --dart-define-from-file=.env
# Project dashboard -> Connect: project URL and publishable key.
# A legacy anon key is also supported by Supabase; never use service_role.
SUPABASE_URL=
SUPABASE_PUBLISHABLE_KEY=
